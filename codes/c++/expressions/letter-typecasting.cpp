#include <iostream>
using namespace std;

int main() {
  cout << (char) ('a'+3 );
  cout << (char) ('a'+0 );
  cout << (char) ('a'+1 );
  cout << (char) ('a'+1 );
  cout << (char) ('a'+11);
  cout << (char) ('a'+4 );
  return 0;
}
