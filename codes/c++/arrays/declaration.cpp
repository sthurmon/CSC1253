#include <iostream>
using namespace std;

int main() {
  // Declare/initialize without size
  int a=1, b=2, c=3;
  string k = "kitty";
  int A[] = {a, b, c};
  /*
  int a[]  = {'a', true, 3.14};
  cout << a[0] << a[1] << a[2] << endl;

  // Declare/initialize with size
  int b[3] = {1, 2, 3};

  // Declare then initialize
  int c[3];
  c[0] = c[1] = c[2] = 0;

  // Cannot do this:
  int d[];
  d[0] = d[1] = d[2] = 0;
  */
}
