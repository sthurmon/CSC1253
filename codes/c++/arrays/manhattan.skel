#include <iostream>
#include <cmath>
#include <array>

___ manhattan(___ a[], ___ b[], ___ n) {
  ___ s=0;
  for (int i=_; _<_; _++)
    _ += std::abs(_[_]-_[_]);
  return _;
}

int main() {
  int a[] = {1, 2, 3, 4, 5};
  int b[] = {5, 4, 3, 2, 1};
  std::cout << manhattan(a, b, _);
}
